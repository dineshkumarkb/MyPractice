from setuptools import setup

setup(
   name='tasks',
   version='1.0',
   description='A useful module',
   author='Dinesh Kumar',
   author_email='foomail@foo.com',
   packages=['tasks'],  #same as name
   install_requires=['numpy']
)